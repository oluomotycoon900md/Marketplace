# Copy this file to .env.local and fill in once you have a real backend/payment provider.
# Anything prefixed NEXT_PUBLIC_ is sent to the browser -- never put a real secret there.
STRIPE_SECRET_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
